package engine3d;

/**
 * This enumeration contains all the possible rendering types that the 3D engine supports.
 *
 * @class RenderFlag
 * @author Sergio Martí Torregrosa
 * @date 18/08/2020
 */
public enum RenderFlags {
    RENDER_WIRE,
    RENDER_FLAT,
    RENDER_SMOOTH_FLAT,
    RENDER_TEXTURED,
    RENDER_FULL_TEXTURED,
    RENDER_TEXTURED_SHADOW,
    RENDER_FULL_TEXTURED_SHADOW,
}
